{
  "name": "obsidian-ledger-light",
  "version": "0.1.0",
  "description": "Simple hledger integration for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "npm run build -- --watch",
    "build": "tsc -p tsconfig.json && cp src/styles.css dist/"
  },
  "dependencies": {
    "obsidian": "^0.15.0"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "typescript": "^4.7.4"
  }
}
