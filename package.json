{
  "name": "obsidian-ledger-light",
  "version": "0.3.0",
  "description": "Simple hledger integration for Obsidian",
  "main": "main.js",
  "scripts": {
    "build": "node esbuild.config.mjs && cp src/styles.css .",
    "test": "vitest run --reporter tree",
    "test:watch": "vitest"
  },
  "dependencies": {
    "obsidian": "^0.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.37",
    "esbuild": "^0.27.4",
    "typescript": "^4.7.4",
    "vitest": "^4.1.0"
  }
}
